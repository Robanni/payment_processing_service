# Payment Processing Service

Асинхронный микросервис для обработки платежей.

## Запуск

```bash
cp .env.example .env
docker compose up --build
```

API — `http://localhost:8000`, RabbitMQ management — `http://localhost:15672` (guest/guest).
